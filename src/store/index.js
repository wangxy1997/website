import Vue from 'vue'
import vuex from 'vuex'

Vue.use(vuex)
const store = new vuex.Store({
  modules: {
    // app: app,
    // tab: tab,
    // iframe: iframe,
    // user: user,
    // menu: menu
  }
})

export default store
